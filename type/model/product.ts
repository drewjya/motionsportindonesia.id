export type Product = {
  product_name: string;
  price: string;
  status: string;
  image_link: string;
};

export type Gallery = {
  url: string;
  title: string;
  preview: string;
  tanggal: string;
};
